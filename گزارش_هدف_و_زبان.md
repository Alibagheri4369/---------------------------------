# گزارش هدف و زبان‌های برنامه‌نویسی پروژه EDX CRM WEB FOV

## ۱. هدف پروژه و کاربردها

EDX CRM WEB FOV (Web Developer Project Manager) یک وب‑برنامه جامع و آماده‑production است که برای **توسعه‑دهندگان نرم‌افزار، تیم‌های توسعه و فریلنسرها** طراحی شده تا **کامل دورة زندگی پروژه‌های وب و نرم‌افزار** را مدیریت کند. در نتیجه، این سیستم به‌عنوان یک پلتفرم «همه در یک» عمل می‌کند و موارد زیر را پوشش می‌دهد:

- **مدیریت پروژه از پیشنویسی تا تحویل**: شاملcreatenویسی و قالب‌هایContract، ۲۵ فاز ساختاری (برنامه‌ریزی، معماری، توسعه، QA، ایمنی، استقرار، پشتیبانی) و تا صورتحساب نهایی.
- **برنامه‌ریزی معماری و فنی**: تعریف پشته فناوری، انتخاب فریم‌ورک، طراحی پایگاه‌داده، طراحی API و ادغام سرویس‌های شخص ثالث.
- **ضمانت کیفیت و ایمنی**: چک‌لیست‌های پایبندی با OWASP، تست عملکرد، سئو، اسکن آسیب‌پذیری.
- **عمليات تجاری**: مدیریت مشتریان، صورتحساب، موتور قیمت‌گذاری، پیگیری پرداخت و گزارش‌های مالی.
- **همکاری تیم** (آماده برای مستقبل): اعضای پروژه، دسترسی مبتنی بر نقش و لاگ فعالیت.
- **افرادپرودукتیویتی**: داشبوردهای سفارشی، ترجیحات تم/زبان، شورتکات‌های کلید و وضعیت‌های UI آفلاین (در حالی که داده‌ها در ابری maintenged می‌شوند).
- **Cutting‑edge AI‑assisted decision making** (اختیاری): تحلیلگر هوش مصنوعی برای تحلیل ریسک، پیشنهادات architectures و تولید قطعه‌های کد.

سیستم به‌صورت **server‑first** ساخته شده؛ تمام داده‌ها (پروژه‌ها، تسک‌ها، ترجیحات، صورتحساب، متادیتا) به‌صورت منحصراً در یک پایگاه داده PostgreSQL ایمن و isolated از طریق Supabase ذخیره می‌شوند و با استفاده از سیاست‌های سطح سطری (RLS) جداسازی ‌strict بین کاربران اعمال می‌گردد. هیچ داده پایداییم در مرورگر (localStorage، sessionStorage، IndexedDB) ذخیره نمی‌شود؛ بنابراین اطلاعات حساس تا حد ممکن در حساب Supabase هر کاربر به allein می‌ماند.

## ۲. زبان‌ها و فناوری‌های برنامه‌نویسی

### فرانت‌اند
- **TypeScript** (≈۵.۸) – زبان اصلی برنامه‌نویسی با تایپ استاتیک و تجربه توسعه بهتر.
- **React** (^۱۹٫۰) – کتابخانه UI برای ساخت مبتنی بر کامپوننت.
- **Vite** (^۶٫۲) – ابزار بيلد و سرور توسعه.
- **Tailwind CSS** (^۴٫۱) – فریمورک CSS کمکی-first برای استایل‌دهی.
- **HTML5** – زبان نشانه‌گذاری.
- **Lucide React** – مجموعه آیکون.
- **Framer Motion** – کتابخانه انیمیشن.
- **React Hot Toast** – سیستم اعلان.
- **Vitest** – چارچوب تست یکپارچه و واحد.
- **Testing Library** – ابزارهای تست DOM.

### بک‌اند / زیرساخت
- **Supabase** – ارائه می‌دهد:
  - پایگاه داده PostgreSQL با قابلیت گسترش.
  - سرویس احراز هویت (ایمیل/رمز عبور، Google OAuth، تأیید ایمیل، بازگردانی رمز عبور).
  - سیاست‌های سطح سطری (RLS) برای جداسازی داده.
  - ایجاد خودکار پروفایل از طریق trigger.
  - توابع لبه (Edge Functions) اختیاری برای منطق سرور-ساید.
- **SQL** – تعریف Scheme دیتابیس (supabase‑schema.sql).
- **dotenv** – مدیریت متغیرهای محیطی.

### توسعه و DevOps
- **Git** – کنترل نسخه.
- **Bun** (اختیاری) – زمان اجرا/JavaScript جایگزین.
- **ESBuild** – بسته‌بندی سریع برای تولید.
- **ESLint** / **TypeScript compiler** – لینت و بررسی نوع.
- **Vercel** – Platform استقرار (مثلاً نشان می‌دهد توسط آدرس vercel `alibexarwebtasker.vercel.app`).

### یکپارچه‌سازی هوش مصنوعی (اختیاری)
- `@google/genai` – SDK هوش مصنوعی jetن‌های Google برای ویژگی‌های کمکی هوش مصنوعی (تحلیل ریسک، مشاوره معماری، کمک به کدنویسی).

*گزارش بر اساس بررسی سورس‑کد، فایل‌های پیکربندی و شواهد استقرار تا تاریخ ۲۰۲۶‑۰۹‑۱۲ تهیه شده است.*