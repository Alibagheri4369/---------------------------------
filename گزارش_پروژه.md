# گزارش پروژه EDX CRM WEB FOV

## ۱. توضیح پروژه

EDX CRM WEB FOV (Web Developer Project Manager) یک وب‑برنامه جامع و آماده‑production است که برای توسعه‑دهندگان نرم‌افزار، تیم‌های توسعه و فریلنسرها طراحی شده تا كامل دورة زندگی پروژه‌های وب و نرم‌افزار را مدیریت کند. این سیستم مدیریت پروژه، برنامه‌ریزی arquitectura، تضمین کیفیت، соответ amniyyat و عملیات تجاری را در یک پلتفرم یکپارچه جمع‌آوری می‌کند.

به‌گونهٔ **server‑first** ساخته شده، این برنامه تضمین می‌کند که تمام داده‌های کاربران شامل پروژه‌ها، تسک‌ها، ترجیحات، صورتحساب و متادیتای مربوطه به صورت منحصراً در یک پایگاه داده PostgreSQL ایمن و ازolated از طریق Supabase ذخیره شوند و با استفاده از سیاست‌های سطح سطری (RLS) جداسازی‌strict بین کاربران اعمال گردد. هیچ داده پایداییم در مرورگر (localStorage، sessionStorage، IndexedDB) ذخیره نمی‌شود؛ таким образом، اطلاعات حساس تا حد ممکن در حساب Supabase هر کاربر به allein بمانند.

پلتفرم رابط کاربری چندزبانه (فارسی، انگلیسی، آلمانی)، سفارشی‌سازی تم (روسیه/تیره/سیستم و چند 팔레ت رنگی) و ترجیحات کاربر پایدار را که پس از رفرش صفحه و تغییر دستگاه نیز حفظ می‌شوند، پشتیبانی می‌کند.

## ۲. زبان‌ها و فناوری‌های استفاده‌شده

### فرانت‌اند
- **TypeScript** (≈۵.۸) – زبان اصلی برنامه‌نویسی با تایپ استاتیک و تجربه توسعه بهتر.
- **React** (^۱۹٫۰) – کتابخانه UI برای ساخت مبتنی بر کامپوننت.
- **Vite** (^۶٫۲) – ابزار بيلد و سرور توسعه.
- **Tailwind CSS** (^۴٫۱) – فریمورک CSS کمکی-first برای استایل‌دهی.
- **HTML5** – زبان نشانه‌گذاری.
- **Lucide React** – مجموعه آیکون.
- **Framer Motion** – کتابخانه انیمیشن.
- **React Hot Toast** – سیستم اعلان.
- **Vitest** – چارچوب تست یکپارچه و واحد.
- **Testing Library** – ابزارهای تست DOM.

### بک‌اند / زیرساخت
- **Supabase** – عرضه می‌کند:
  - پایگاه داده PostgreSQL با قابلیت گسترش.
  - سرویس احراز هویت (ایمیل/رمز عبور، Google OAuth، تأیید ایمیل، بازگردانی رمز عبور).
  - سیاست‌های سطح سطری (RLS) برای جداسازی داده.
  - ایجاد خودکار پروفایل عبر trigger.
  - توابع لبه (Edge Functions) اختیاری برای منطق سرور-ساید.
- **SQL** – تعریف Scheme دیتابیس (supabase‑schema.sql).
- **dotenv** – مدیریت متغیرهای محیطی.

### توسعه و DevOps
- **Git** – کنترل نسخه.
- **Bun** (اختیاری) – زمان اجرا/JavaScript جایگزین.
- **ESBuild** – بسته‌بندی سریع برای تولید.
- **ESLint** / **TypeScript compiler** – لینت و بررسی نوع.
- **Vercel** – Platform استقرار (مثلاً نشان می‌دهد توسط آدرس vercel `alibexarwebtasker.vercel.app`).

### یکپارچه‌سازی هوش مصنوعی (اختیاری)
- `@google/genai` – SDK هوش مصنوعی jetن‌های Google برای ویژگی‌های کمکی هوش مصنوعی (تحلیل ریسک، مشاوره معماری، کمک به کدنویسی).

## ۳. هدف و کاربرد پروژه

هدف اصلی EDX CRM WEB FOV این است که تکه‌تکه‌کاری ابزارهای معمولاً توسط توسعه‌دهندگان وب به کار رفته را رفع کند و یک پلتفرم «همه در یک» ارائه دهد که شامل موارد زیر باشد:

1. **مدیریت کامل دورة حياه پروژه** – از پیشنویسی و قالب‌های قرارداد (منظور قالب‌ها) تا ۲۵ فاز ساختاری (برنامه‌ریزی، معماری، توسعه، QA، ایمنی، استقرار، پشتیبانی) تا تحویل نهایی و صورتحساب.
2. **برنامه‌ریزی معماری و فنی** – ماژول‌های تعریف پشته فناوری، انتخاب فریم‌ورک، طراحی پایگاه‌داده، طراحی API و ادغام سرويس‌های شخص ثالث.
3. **ضمانت کیفیت و ایمنی** – چک‌لیست‌های داخلی برای پایداری با OWASP، تست عملکرد، سئو، اسکن آسیب‌پذیری.
4. **عمليات تجاری** – مدیریت مشتریان، صورتحساب، موتور قیمت‌گذاری، پیگیری پرداخت و گزارش‌های مالی.
5. **همکاری تیم** – (آماده برای مستقبل) اعضای پروژه، دسترسی مبتنی بر نقش و لاگ فعالیت.
6. **افرادپرودукتیویتی** – داشبوردهای سفارشی، ترجیحات تم/زبان، شورتکات‌های کلید و وضعیت‌های UI آفلاین (در حالی که داده‌ها در ابری maintenged می‌شوند).
7. **ตัดّ تصمیم‌گیری با هوش مصنوعی** – تحلیلگر هوش مصنوعی اختیاری برای تحلیل ریسک، پیشنهادات architectures و تولید قطعه‌های کد.

سیستم به‌صورت явным образом ممنوع است:
- داده‌های جعلی یا مocke در UI.
- bleiben اطلاعات حساسة در طرف کلاینت.
- ایمنی فقط فرانت‌اند (بدون پشتیبان سرور).
- کد یا رمزهای σerit در مخزن.
- دسترسی نامعتبر به مسیرهای محافظت شده.

تمام تغییرات داده از طریق درخواست‌های احراز هویت Supabase جریان می‌یابند و سیاست‌های RLS امکان دسترسی به مواردی که `auth.uid()` به مالک آن تعلق دارد را فقط می‌دهند.

## ۴. خالق

براساس آدرس استقرار (`alibexarwebtasker.vercel.app`), نام دایرکتوری محلی (`Alibexar`) و الگوها در سوابق commit مستندات مرتبط، پروژه توسط **Alibexar** ایجاد و نگهداری شده است. هویت خالق از شواهد فوق استنتاج می‌شود؛ در مخزن repository فیلد صریح نویسنده وجود ندارد.

## ۵. معماری سیستم (به‑صورت خلاصه)

```
+---------------------+       +---------------------+       +---------------------+
|   مرورگر وب (SPA)  | <---> |   Supabase Auth     | <---> | Supabase PostgreSQL |
|  (React + TS)       |       |  (JWT, ایمیل/رمز عبور,   |       |  (جداول RLS-فعال,   |
|                     |       |   Google OAuth)    |       |   Triggers)         |
+---------------------+       +---------------------+       +---------------------+
        ^                           ^                           ^
        |                           |                           |
        |  HTTPS (REST/Realtime)    |  HTTPS (REST/Realtime)    |
        |                           |                           |
        +---------------------------+---------------------------+
                         رمزنگاریencrypted ارتباط (TLS)
```

- **احراز هویت**: به‌صورت کامل توسط Supabase Auth انجام می‌شود؛ پایداری جلسه از طریق کوکی (یا JWT در حافظه) مدیریت می‌شود – **هیچ** استفاده از `localStorage`/`sessionStorage` برای توکن احراز هویت.
- **لایه دسترسی داده**: همه تعاملات با پایگاه داده از طریق ماژول‌های سرویس (مثلاً `projectService.ts`, `taskService.ts`, `authService.ts`) که از مشتری JS Supabase استفاده می‌کنند انجام می‌شود.
- **اجرای RLS**: هر جدول سیاست‌هایی دارد که عمل‌های `SELECT`, `INSERT`, `UPDATE`, `DELETE` را به سطرهایی محدود می‌کند که `auth.uid() = user_id` (یا بررسی‌های روابطی مناسب) برقرار باشد.
- **قابلیت real-time (اختیاری)**: Supabase Realtime می‌تواند برای به‌روزرسانی زنده (مثلاً تکمیل تسک، وضعیت صورتحساب) مشترک شود.
- **قدرت هوش مصنوعی**: در صورت فعال‌سازی، frontend با API هوش مصنوعی Google از طریق کلید امن که فقط در متغیرهای محیطی ذخیره می‌شود (در کلاینتobecsh نیست) ارتباط برقرار می‌سازد.

## ۶. ویژگی‌های کلیدی

- **عزلetí کاربر**: داده هر کاربر از طریق UID احراز هویت Supabase و RLS criptוגרافiquement izolyatsiya shode; دسترسی 교차‑کاربر بدون صراحت-sharing غیرممکن است.
- **ترجیحات پایدار**: تم، زبان، منطقه زمانی، فرمت تقویم و צפיפות رابط در جدول `user_preferences` ذخیره می‌شوند و هنگام ورود بازیابی گردند.
- **پشتیبانی چندزبانه**: پشتیبانی کامل از فارسی (fa)، انگلیسی (en) و آلمانی (de) با چیدمان RTL برای فارسی.
- **موتور تم**: حالت‌های روشن/تیره/سیستم بالإضافة به هشت تم رنگ emphasize (پیش‌فرض، آبی، نیلی، بنفش، زمردی، فیروزی، نارنجی، گلابی، سیاهی).
- **مدیریت پروژه و تسک**: ایجاد، ویرایش، حذف پروژه‌ها و تسکس؛ تعیین اولویت، تاریخ سر رسید، وضعیت؛ تخصیص تسک به کاربران؛ ردیابی پیشرفت.
- **صورتحساب و صورتحساب**: تولید صورتحساب با موارد détaillé، مالیات، تخفیف، تولید PDF (از طریق توابع ب voluntarily یا کتابخانه кліентская) و پیگیری پرداخت.
- **لاگ فعالیت**: تمام اقدامات مهم (ورود، ایجاد پروژه، به‌روزرسانی تسک) در یک لاگ تدوین نامен変更を記録 می‌شود.
- **تحلیلات**: امکان ردیابی رویدادهای سفارشی (مثلاً استفاده از ویژگی، رخداد خطا) برای بهبود محصول.
- **Cabeçalhos de segurança**: CSP، X‑Frame‑Options، Referrer‑Policy و غیره از طریق Vercel/_headers یا همان‌گونه تنظیم می‌شوند.
- **ساخت تولید بهینه**: بسته‌بندی با code‑splitting، بارگذاری inú‌‌المسارات، و imprонт asset‑fingerprinting.

## ۷. استقرار و محیط

برنامه برای استقرار در Vercel (یا هر host Node‑سازگار) تنظیم شده است. متغیرهای محیطی ضروری:

| متغیر                           | توضیح                                        | مثال                                   |
|----------------------------------|----------------------------------------------|----------------------------------------|
| `VITE_SUPABASE_URL`              | آدرس پروژه Supabase                         | `https://xyzcompany.supabase.co`       |
| `VITE_SUPABASE_ANON_KEY`         | کلیت عمومی/anonim Supabase                  | `public-anon-key`                      |
| `APP_URL`                        | آدرس پایه برای بازگشت OAuth و لینک‌های ایمیل | `http://localhost:3000` (توسعه)       |
| `GEMINI_API_KEY`                 | (اختیاری) کلید برای ویژگی‌های هوش مصنوعی Google | `AIza…`                                |

فایل `.env.local` (در Git ignore) این مقادیر را در زمان اجرا فراهم می‌آورد. Scheme Supabase باید از طریق SQL Editor قبل از اولین استفاده اعمال شود.

## ۸. نتیجه‌گیری

EDX CRM WEB FOV راه‌حلی بالغ، ایمن و غنی از ویژگی‌هاست برای توسعه‌دهندگان وب که به دنبال یک پلتفرم یکپارچه برای مدیریت پروژه‌های خود از گُمれば تا تحویل می‌ängen. پایبندی به ذخیره‌سازی سرور، احراز هویت سخت‌گیرانه، سطح سطری güven (RLS) و مجموعه omfattend chứcショナル، این سیستم برای فریلنسرهای مستقل و additionally برای تیم‌های توسعه کوچک مناسب است. معماری دوستانه به منبع (TypeScript، React، Supabase) امکان گسترش و سفارشی‌سازی بیشتری را دارد در حالی که استاندارد بالایی از کیفیت کد و ایمنی حفظ می‌شود.

*گزارش بر اساس بررسی سورس‑کد، فایل‌های پیکربندی و شواهد استقرار تا تاریخ ۲۰۲۶‑۰۹‑۱۲ تهیه شده است.*